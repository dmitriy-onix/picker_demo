/// Represents the source from which media (images or files) can be picked.
enum MediaSource {
  /// Pick image from device camera
  camera,

  /// Pick image from device gallery/photos
  gallery,

  /// Pick file from device file system
  fileSystem,
}
